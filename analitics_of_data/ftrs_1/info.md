# Feature Smoothing & Correlation Analysis Research

## 📌 Обзор исследования

Цель данного этапа — определить оптимальные методы трансформации сырых признаков для максимизации их прогностической способности относительно целевых показателей выбросов ($NO, NO_2, SO_2, CO$).

В ходе исследования сравнивались три подхода:

1. **Original**: Использование сырых данных без изменений.
2. **Rolling Mean**: Простое скользящее среднее с окнами от 5 до 100 периодов.
3. **EWM (Exponential Weighted Moving Average)**: Экспоненциальное сглаживание, где свежие данные имеют больший вес.

## 📊 Ключевые выводы

На основе анализа файла `smoothing_metrics_correlation_analysis.csv` выявлены следующие закономерности:

### 1. Доминирование EWM

Для большинства критически важных признаков (например, `V_ST`, `t_avg2`, `raz_avg`) **экспоненциальное сглаживание (EWM)** показало лучшие результаты корреляции, чем обычное скользящее среднее. Это указывает на то, что динамика процесса важнее, чем простое усреднение за период.

### 2. Лидеры корреляции

Наибольшую значимость для модели представляют следующие связки:

* **V_ST (Volume/Velocity ST)** + `ewm_14` $\rightarrow$ корреляция до **0.80** с $SO_2$ и **0.77** с $CO$.
* **t_avg2 (Temperature)** + `ewm_100` $\rightarrow$ корреляция **0.43** с $CO$.
* **f_avg2** + `rolling_100` $\rightarrow$ показал колоссальный **Gain (прирост) +0.31**, что превратило слабый признак в значимый.

### 3. Эффект сглаживания (Gain)

Для многих признаков переход от `Original` к `ewm` или `rolling` позволил поднять корреляцию на **5-15%** (в абсолютных значениях). Это подтверждает гипотезу о высоком уровне высокочастотного шума в сырых данных датчиков.

## 🛠 Методология

Для каждого признака из набора данных был проведен «турнир» трансформаций:

1. Вычислялись корреляции Пирсона для всех вариантов сглаживания.
2. Фиксировалась метрика `Max_Abs_Corr` (максимальная абсолютная корреляция).
3. Рассчитывался `Gain` — разница между сглаженным и сырым признаком.

## 🚀 Дальнейшие шаги

На основе этого исследования в основной пайплайн генерации признаков (`FeatureEngineer`) будут внедрены:

* Автоматическая замена сырых признаков на их `ewm` версии с оптимальным окном (span).
* Добавление признаков-разностей (Momentum): `(Original - EWM)`, для фиксации резких отклонений от тренда.